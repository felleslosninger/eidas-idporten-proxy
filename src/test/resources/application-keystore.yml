---
eidas:
  oidc-integrations:
    idporten:
      client-auth-method: private_key_jwt
      client-secret:
      key-id: okcomputer
      keystore: #to authenticate towards maskinporten
        keystore-type: JKS
        keystore-location: classpath:cp-contactinfo-ver2.jks
        keystore-password: secret
        key-alias: cp-contactinfo
        key-password: secret

